
# Програма запитує кількість розділів, їх назви, сортує, видаляє дублікати
# та виводить результат.

# функція для вибору способу сортування
def choose_sort_method():
    print("Оберіть метод сортування:")
    print("1 - за зростанням (A → Z)")
    print("2 - за спаданням (Z → A)")
    print("3 - без сортування")
    
    while True:
        choice = input("Ваш вибір (1/2/3): ")

        # перевіряємо, чи введене число та чи воно у дозволеному діапазоні
        if choice.isdigit() and choice in ["1", "2", "3"]:
            return int(choice)
        else:
            print("Помилка! Введіть 1, 2 або 3.")

# головна програма
while True:
    count = input("Введіть кількість розділів (1-20): ")

    # перевірка, що число ціле, не від'ємне і не дробове
    if count.isdigit():
        count = int(count)
        if 1 <= count <= 20:
            break
        else:
            print("Помилка! Число повинно бути від 1 до 20.")
    else:
        print("Помилка! Введіть ціле число.")

sections = []  # список для збереження назв розділів

# введення назв розділів
for i in range(count):
    section_name = input(f"Введіть назву розділу №{i + 1}: ")
    sections.append(section_name)

# видалення дублікатів через множину та повернення назад до списку
sections = list(set(sections))

# отримуємо метод сортування
sort_method = choose_sort_method()

# сортування згідно вибору
if sort_method == 1:
    sections.sort()
elif sort_method == 2:
    sections.sort(reverse=True)

# вивід результату
print("\nСписок розділів:")
for item in sections:
    print("-", item)
